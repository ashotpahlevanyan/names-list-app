import React from 'react';

import { StyledHeader } from './styled';

export const Header = () => {
    return (
        <StyledHeader>
            <h1>Names List App</h1>
        </StyledHeader>
    );
};
